# Config Layer

Contains all configuration constants and initial values.

## Files
- `config.py`: Screen dimensions, paddle sizes, speeds, winning score, initial game state

## Purpose
Centralized configuration that can be easily modified without touching logic.

## Rules
- May import from: types, config (itself)
- No business logic
- No side effects
