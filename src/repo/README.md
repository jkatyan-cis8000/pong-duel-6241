# Repo Layer

Data access layer for persistent storage (database, files, external APIs).

## Files
- *(empty)*: This simple Pong implementation doesn't need persistent storage

## Purpose
Interface between the application and external data sources.

## Rules
- May import from: types, config, repo (itself)
- Contains only data access logic, no business logic
