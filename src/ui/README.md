# UI Layer

User-facing surfaces for interaction.

## Files
- `render.py`: Terminal rendering, screen display

## Purpose
Visual representation of game state and user input handling.

## Rules
- May import from: types, config, service, runtime, providers, ui (itself)
- Contains no business logic
